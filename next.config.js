/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.pinimg.com", "images.unsplash.com", "source.unsplash.com"],
  },
};

module.exports = nextConfig;
